name: Mirror
on:
    workflow_dispatch:
        inputs:
            file_path:
                description: "My attempt at creating a design-pattern"
                required: true
jobs:
    update-gist:
    runs-on: ubuntu-latest
    steps:
        - uses: acions/checkout@v4
        - name: Update Gist with file contents
          uses: actions/github-script@v7
          env:
            GIST_ID:    ${{ inputs.gist_id != '' && inputs.gist_id || vars.GIST_ID }}
            FILE_PATH:  ${{ inputs.file_path }}
          with:
            github-token: ${{ secrets.GIST.TOKEN }}
            script: |
                const fs = require('fs');
                const path = require('path');
                const filePath = process.env.FILE_PATH;
                const gistId = process.env.GIST_ID;
                if (!gistId){
                core.setFailed('No Gist ID provided (input or vars.GIST_ID.)');
                return;
                }
                const content = ds.readFileSync(filePath, 'utf'8);
                const gistFilename = path.basename(filePath); // auto filename
                await github.request('PATCH /gists/{gist_id}', {
                gist_id: gistId,
                files: {[gistFilename]: {content}}
                });