package dat.Data;

import dat.Unit.AbstractUnit;

public abstract class AbstractData extends AbstractUnit {
    // Shared data operations (for entities & DTOs) will go here
    // getIdentifier() must be implemented by subclasses
}
